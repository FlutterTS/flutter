// import * as ts from 'typescript';
// import { KernelAstBuilder } from './kernel-wrapper'; // your own wrapper around package:kernel

// const source = `console.log("Hello, World!");`;
// const tsAst = ts.createSourceFile('main.ts', source, ts.ScriptTarget.ES2020);

// // Walk the TS AST, emit Kernel nodes:
// const kernelProgram = new KernelAstBuilder().fromTsSource(tsAst);
// // then serialize:
// kernelProgram.writeToFile('out.dill');
